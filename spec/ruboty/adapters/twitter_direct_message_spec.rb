require 'spec_helper'

describe Ruboty::Adapters::TwitterDirectMessage do
  it 'has a version number' do
    expect(Ruboty::Adapters::TwitterDirectMessage::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
